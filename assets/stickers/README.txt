Drop custom sticker images (PNG with transparency works best) into this folder.
The site uses animated emoji stickers by default — to use an image instead,
replace the emoji in CONFIG (script.js) with an <img> tag, e.g.:

  icon: '<img src="assets/stickers/star.png" alt="" style="width:2em">'
